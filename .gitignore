node_modules/

# Nuxt build artifacts
.nuxt/
.output/
.data/

# Per-guide working files (candidates, contact sheets, raw sources) — not committed.
# The curated manifest.json IS committed so rebuilds need no network.
guides/*/work/
guides/*/sheets/
**/*.src

# Verify artifacts
build/.verify/
*.screenshot.png

# Local env / secrets (API keys for image sources)
mise.local.toml

# OS
.DS_Store
